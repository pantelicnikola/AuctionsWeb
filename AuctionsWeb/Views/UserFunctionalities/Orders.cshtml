@model ICollection<Order>

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">Number of tokens</th>
            <th scope="col">Price</th>
            <th scope="col">State</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.NumTokens</td>
                <td>@order.Price</td>
                <td>@order.State</td>
            </tr>
        }
    </tbody>
</table>